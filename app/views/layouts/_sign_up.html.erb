<div class="w3-container">
  <h1>Sign Up</h1>
  <p>This form is reserved for admin purposes only.</p>
  <hr>

  <%= form_for(@user) do |f| %>

    <%= render 'shared/error_messages' %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :email %>
    <%= f.text_field :email%>

    <%= f.label :password %>
    <%= f.password_field :password%>

    <%= f.label :password_confirmation, "Confirmation" %>
    <%= f.password_field :password_confirmation %>

    <%= f.submit "Create my account", class: "btn btn-primary" %>
    <a href="<%= root_url %>" class="btn btn-primary w3-right">Cancel</a>
  <% end %>

</div>
</div>